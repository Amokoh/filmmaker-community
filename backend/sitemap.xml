const fs = require("fs");
const path = require("path");

const websiteUrl = "https://storysharestudio.com"; // Replace with your actual domain

const pages = [
  "/",
  "/masterclass",
  "/opportunities",
  "/blog",
  "/community",
  "/contact",
]; // Add all your important pages

const sitemapContent = `<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  ${pages
    .map(
      (page) => `
    <url>
      <loc>${websiteUrl}${page}</loc>
      <lastmod>${new Date().toISOString()}</lastmod>
      <changefreq>weekly</changefreq>
      <priority>0.8</priority>
    </url>`
    )
    .join("")}
</urlset>`;

fs.writeFileSync(path.join(__dirname, "public", "sitemap.xml"), sitemapContent, "utf8");

console.log("✅ Sitemap generated: /public/sitemap.xml");
